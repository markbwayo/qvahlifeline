# 05 — Decisions Log

Append a line whenever a real decision is made. Newest at the bottom.

| ID | Decision | Why |
|----|----------|-----|
| D-001 | Ontology doctrine: typed objects + links + hazard propagation + action playbook; deterministic core, AI at edges only | The product IS the semantic model; wrong impacts/actions can cost lives — must be exact, explainable, testable |
| D-002 | SQLite two-table graph (objects, links) + pure-Python BFS; no graph DB, no networkx at MVP | District-scale graphs are small; keep installs trivial and the engine readable |
| D-003 | Port 8017 at /opt/lifeline, repo markbwayo/qvahlifeline, git-only workflow | Next free Qvah port; standard plumbing |
| D-004 | Pilot area decided by a Day-1 data check, not preference: candidate A = Marsabit/Isiolo corridor (research-verified coverage, Kenya NDMA relevance), candidate B = Mt Elgon corridor Mbale–Manafwa–Bududa (founder story, flood+landslide history). Check OSM asset density + GloFAS river points for both; pick the one that demos better; log result here | Depth in one convincing area beats breadth; don't ingest before locking |
| D-004 (result) | Day-1 data check run 2026-07-05 on VPS. B (Mt Elgon: Mbale–Manafwa–Bududa) wins decisively: 1107 assets/100km² vs 119 (Isiolo A); crossings 490 vs 156; usable GloFAS points 2 (peak ~7 m³/s) vs 0. Isiolo reads 0.0 m³/s at all river points — arid/drought corridor, no modeled perennial discharge, wrong hazard for a riverine-flood MVP. Founder authenticity (Mbale/Elgon, Lumasaba) reinforces. **PILOT REGION LOCKED = Mt Elgon.** | Data and story point the same way; a riverine-flood MVP needs a modeled river network, which only Elgon has |
| D-005 | Hazard feeds: GloFAS via Open-Meteo flood API + CHIRPS; triggers are watch/alert screening levels, stated honestly | Free, no-key, daily; 5 km resolution means we prioritise and explain, not predict per-metre |
| D-006 | Every impact stores its why-chain (ordered hazard→object link path); UI must render it | Explainability is the differentiator vs dashboards and vs black-box AI |
| D-007 | Playbook and fragility rules are versioned data (CSV / one table), never buried in code | Committees must be able to read, challenge, and tune them |
| D-008 | Demo must run with USE_LIVE=0 (cached/seeded data) | A dead network can never kill the pitch or the video |
| D-009 | Message templates aligned to CAP fields; English + one local language (AI-drafted, human-approved, marked as draft) | National adoption path + last-mile reality; AI never decides content of the impact, only wording |
| D-010 | Scope guard: submission on 30 July, one day early; any feature threatening that is cut and logged | Hackathons are lost to deadline slippage more than to missing features |
| D-011 | Post-event feedback capture (what actually happened per asset) is in the roadmap/pitch but NOT built for MVP | It's the moat later; it's scope creep now |
| D-012 | No Anthropic API in production; edge model default = Gemini free tier behind one adapter function | Cost + policy consistent across Qvah |
| D-013 | Ingest bbox ≠ data-check bbox. The 1108 km² check box holds ~12,266 objects (a whole region); ingesting it violates hard rule 4 (one legible district). Pilot tightened to a single river sub-catchment (~150–400 objects) chosen for a clean single-point-of-failure crossing. Exact corridor + anchor bridge: TBD (Bwayo to name) | Depth beats coverage; the demo must read as one district with one legible dependency chain |
| D-014 | Pilot corridor locked = Manafwa River @ Bubulo (Manafwa town); tight ingest bbox S0.884/W34.264/N0.929/E34.309 (~25 km²). Single-point-of-failure crossing NOT pre-named — selected by engineering judgement from the ingested crossings list. | No reliable named-bridge recall; data-first SPOF selection is more defensible and matches D-004 doctrine. Bbox provisional, tuned to the 150–400-object legibility target from ingest counts |
| D-015 | ingest_osm.py Step A = objects only, zero links. Crossings stored as type "bridge" + props.structure (bridge/culvert/ford/causeway) to match ontology.FRAGILITY. Full way geometry stored in props_json for Step B spatial inference. Idempotent wipe+reload. | Link inference feeds propagation and needs test cases (rule: nothing touches the engine untested); separating it keeps Step A incapable of producing a wrong impact |
| D-016 | A crossing = a road×river geometric intersection, not only an OSM-tagged bridge. OSM tagged just 2 bridges in the Manafwa box (both off-town, no culverts/fords). Step B will synthesise crossing objects from road/river geometry intersections; the engineer classifies demo-critical ones (bridge/culvert/ford) to set fragility. | Rural OSM under-tags crossings; the physical model is intersection-based. Geometry finds the crossing deterministically, the human sets the structure — hard rule 1 intact |
| D-017 | Pilot box corrected north to 0.905,34.260,0.962,34.305 (old box topped at 0.929, missed the Manafwa Town Council crossings). Operator ground-truth crossings recorded in data/operator_crossings.csv (source=operator): TC bridge (main road) + north footpath culvert, structures classified on satellite by the engineer. | The demo spine is operator-verified crossings, not OSM tags; rural OSM under-maps crossings, and the footpath culvert will never be in OSM |
| D-018 | A crossing object = a bridge/culvert/ford that CARRIES A VEHICLE ROAD (road_segment highway classes). Footbridges (highway=path) and stream/ditch/drain culverts are excluded at ingest. Rural OSM mapped ~80 such micro-crossings in the box (415 objects, 86 crossings); filtering to vehicle-road crossings gives ~16 real crossings, ~345 objects. Untagged road-over-river crossings recovered geometrically in Step B. Fords kept unfiltered (node-level, classic flood crossings). OSM confirms the operator's TC bridge = "Old Manafwa bridge" (tertiary). | LIFELINE models vehicle lifelines to clinics/markets; a footbridge over a ditch is not one. Keeps the district legible (rule 4) and Step B's graph clean |
| D-019 | Phase 1 Step A complete. Filtered ingest loaded 362 objects for box 0.905,34.260,0.962,34.305: 16 crossings (6 road bridges incl. Old Manafwa bridge/tertiary = operator TC bridge; 10 fords), 4 clinics, 83 settlements, 20 schools, 85 river reaches, 154 roads. --refresh needed due to box-only cache key (fix pending). | Clean, legible district graph; demo spine (TC bridge) OSM-confirmed |
| D-020 | Hardened ingest_osm.py: (a) Overpass cache key includes a hash of the query text, so a changed query auto-refetches and stale-query results can't silently return; (b) each bucket retries across 2 mirrors with exponential backoff (5/10/20s) and isolates on failure (returns FAILED, no crash) — successful buckets stay cached so a re-run retries only failures; (c) a real load aborts (exit 1) rather than write a graph missing a whole category. --refresh now rarely needed. | The box-only cache key served the stale 86-crossing set; one slow bucket crashed a full run. Both traps would bite Step B, which refetches |
| D-021 | Map renders road_segment + river_reach as polylines from stored props.geometry (rivers blue; roads grey when OK, state-coloured and thicker when impacted); point assets + crossings stay as dots on top. No propagation/fragility change. | 362 dots was illegible; lines make it read as a district (rule 4) and pre-wire the severed-road visual for Step B |
| D-022 | Manafwa town crossing is TWO real bridges 19.8 m apart on the SAME reach w188321163: w128611448 (Manafwa Bridge, B112 secondary, main tarmac) and w902422828 (Old Manafwa bridge, C870 tertiary). The demo SPINE = w128611448 (main tarmac; operator satellite point falls on it) — corrects earlier docs that named "Old Manafwa bridge". Old C870 kept as an OSM vehicle crossing. Clustered-crossing reconciliation: operator_crossings.csv match_hint (OSM id) resolves identity; injection refuses on an ambiguous cluster with no hint | Nearest-pick silently decided a spine's identity; two named bridges with different refs/road-classes are two structures; both co-fail (same reach) so the SPOF is the crossing pair, not one bridge — reachability (sub-step 5) confirms south-bank isolation |
| D-023 | Geometric crossing synthesis: vehicle road×river intersections with no crossing within ~50 m create source=synth, needs_review=True, structure-less candidates (provenance stored). Structure/fragility never guessed, so they yield no impact until a human classifies them. Vehicle highway classes only; ~50 m self+existing dedup; idempotent | Rural OSM under-tags crossings: a missed crossing risks a false ISOLATED (over-warning), a guessed structure risks a false all-clear (under-warning). Find them by geometry, leave classification to the human. Propagation treatment of unclassified crossings deferred to sub-step 3 |
| D-024 | crosses = nearest river_reach within ~50 m of the crossing point; carries = every vehicle road whose line passes within ~15 m. Footpath crossings get crosses but never carries (vehicle-graph gate). Synth (needs_review) crossings get both, so an unclassified crossing can sever a route. Idempotent (rebuilds only geom_* links). | 15 m is tuned below the 19.8 m town-bridge separation so the two clustered bridges don't cross-link carriers (~5 m margin — verified on the real graph); it still captures both segments of a road that splits at a bridge (~0 m). Whether an unclassified crossing actually severs is the conservative fragility rule, deferred to the propagation step |
| D-025 | Reachability graph inference: connects = vehicle roads sharing a vertex (~1 m grid; footpaths excluded so the network is vehicle-only); access_via = each settlement AND serving facility attaches to its nearest vehicle road; serves = each settlement to the nearest facility of each type within an 8 km catchment cap (committee-editable). All idempotent (rebuild only geom_* links). Verified end-to-end through propagate.py: settlement whose only clinic route crosses a blocked culvert -> ISOLATED. | The BFS needs a vehicle-only road graph, an entry point per settlement/facility, and a catchment so it knows which facility a settlement depends on. Footpath exclusion prevents a footbridge silently keeping a route open |
| D-026 | carries threshold raised 15m->20m: real measurement on the live graph showed the demo-spine bridge w128611448's true nearest road at 17.7 m (excluded by 15 m - a wrong-impact bug, the spine could never sever its road) while the OTHER town bridge's road sat safely at 23.9 m. 20 m sits inside that real gap. Added a nearest-road fallback (cap 100 m, flagged geom_carries_fallback) for any crossing left with zero carriers after the primary pass, so an unlinked crossing can't silently fail to sever a route it's actually on. | A crossing with no carrier can never produce a severed road - if it's the only route to a clinic, propagation would miss the isolation. Threshold tuned from measured data, not guessed; fallback closes the residual risk for less-clustered crossings elsewhere in the district |
| D-027 | Unclassified crossings (structure None/unknown) are scored as the MOST fragile structure (ford), not the least. Fixes bridge_state's `structure or "bridge"` fallback and propagate.py's `props.get("structure","bridge")` default - two layers of the same wrong assumption, which at alert severity returned AT_RISK (non-severing) instead of LIKELY_IMPASSABLE (severing), so a flood over an unclassified crossing severed no road and isolated no village: a false all-clear. Why-chain now declares `assumed_structure:ford(unclassified)`. ontology -> v0.2. | Base rate: an unmapped rural crossing is more likely a ford/culvert than an engineered bridge. Asymmetric cost: a false "may be out" costs an inspection; a false "all clear" can kill. Applies to all 18 synth crossings. Known structures unchanged (an engineered bridge at watch is still OK) - conservative, not indiscriminate |
| D-028 | create_hazard refuses a target_id that isn't an object in the graph, and refuses a non-river_reach target for riverine_flood. require_reach() is mandatory before any hazard insert (including the Phase 2 live scan). | A hazard on a nonexistent target propagates to nothing and reports zero impacts - indistinguishable from "all clear". A typo'd reach id must crash loudly, never reassure |
| D-029 | Demo hazard targets the real reach w188321163 (crossed by BOTH town bridges, so the crossing pair fails together = the true SPOF), resolved via resolve_demo_reach(): DEMO_REACH_ID env -> real reach -> seed R1 -> raise. Demo severity must be `emergency`: both town bridges are structure='bridge' (engineered), which the fragility table scores AT_RISK (non-blocking) at `alert`, so alert severs no road and isolates nobody. | Keeps the seed graph and its tests working while pointing the real demo at real geometry. The severity requirement is the fragility table being honest about engineered bridges, not a bug - the pitch must describe an emergency-level (high return-period) trigger, per D-005's return-period-relative triggers |
| D-030 | propagate.py: (a) a settlement's why-chain names the first severed road on ITS OWN baseline route and the bridge that severed it, not an arbitrary member of the severed set; (b) a severed road is untraversable as a path endpoint, not just mid-path - previously a village whose access road (and whose clinic's access road) was the severed road returned "reachable in 1 hop" and reported NO IMPACT while its road was SEVERED; (c) all BFS traversal iterates sorted containers - set iteration over str keys is not stable across processes, so why-chains were not reproducible (invariant 1). REROUTED impacts now name the alternate route. | (b) was a live false all-clear - the exact failure this product exists to prevent. (a) would have sent an officer to the wrong bridge, destroying the credibility the why-chain is meant to create. (c) broke invariant 1 silently. All three verified by running the new spine suite against the OLD engine: 5 of 10 tests fail, including the all-clear |
| D-031 | Roads carrying the same crossing are mutually `connects` (inferred_by=via_crossing). In OSM the bridge is its own way, which ingest stores as type=bridge, so the approach and continuation roads share no vertex. Measured on the real graph: the road network was 2 disjoint components (72 + 67 segments) and only 10 of 83 settlements had ANY baseline road path to their serving clinic. | Without this no vehicle route in the district ever crosses a river, so the entire south-bank isolation premise was unsupported by the graph. The edge disappears when the crossing blocks, so the engine needs no change |
| D-032 | Reachability is evaluated per facility, per 09 step 4, not on the minimum hop count pooled across all serving facilities. | Pooling hid the loss of a facility: a village that lost its clinic across the river but kept its local school reported NO IMPACT and fired no playbook action. An under-warning |
| D-033 | A facility is SERVICE_AT_RISK only for settlements that had baseline access and lost it. The ISOLATED branch previously added every serving facility. | Produced a spurious SERVICE_AT_RISK on Namuembi Medical Centre for villages that could never reach it. An over-warning; a judge asking "why is that clinic at risk?" would have found no answer |
| D-034 | Blocking a crossing with ≥2 carrier roads cuts only the crossing edge between them; the roads stay traversable. A crossing that is the sole carrier of one road removes that road entirely. | Deleting the whole carrier way made a school on the SAME bank as its village unreachable — a bridge failing does not stop you driving along its approach road. The single-carrier case cannot be localised without splitting ways at crossings (post-hackathon), so it over-states the break, which is the safe direction |
| D-035 | A crossing joining ≥2 roads is a distinct OSM way (the bridge deck): the CROSSING takes the impassable state, the network edge between its approach roads is cut, and the approach roads take NO state. Only a crossing that is the sole carrier of one unsplit road SEVERS that road (dropped whole). | The live demo printed "alternate_via: …>w169219432>…" where w169219432 was itself SEVERED — telling an officer "this road is cut, now drive it". The bridge deck is the impassable element; approach roads stay drivable. Guarded by a test that no alternate route traverses a severed road |
| D-036 | Hazard gains scope (reach|river). river = trigger reach + all vertex-connected reaches of the same `waterway`. Streams excluded (GloFAS does not resolve them). Demo uses demo_flood_river(). hazards.scope column added by an idempotent migration. | Reach scope let 43 settlements detour across crossings a real Manafwa flood would also close: 0 ISOLATED. River scope gives 62 ISOLATED / 2 clinics at risk. Sensitivity checked: adding the 6 touching streams moves ISOLATED 62→63, adding the other named rivers moves it not at all — the result is topology, not over-warning |
| D-037 | db.conn() self-heals the schema: it runs SCHEMA + additive column migrations once per process per DB path, so a fresh connection is always current even if init() was never called. | The `scope` column (D-036) only got created inside init(); the live VPS database predates it and no ad-hoc script calls init(), so create_hazard crashed with "table hazards has no column named scope" — a migration that depends on someone remembering to run it will be missed by scripts, cron, and the live scheduler. Regression-tested: a legacy table with no `scope` column, no init() call, straight to create_hazard, must still succeed |
| D-038 | The two town bridges co-fail (same reach `w188321163`) and are therefore never alternates for each other. In the live demo `w902422828` (Old Manafwa bridge) is LIKELY_IMPASSABLE yet appears in **0 of 62** isolated why-chains; `w128611448` (Manafwa Bridge B112) appears in **51 of 62**. The *failure* is a crossing pair; the *dependency* is one deck. Refines D-022. | Every officer and judge asks "there's a second bridge 20 m away — reroute over it." Reachability answers without being told: same reach, same flood, never an alternate |
| D-039 | Demo headline claim = **"62 settlements lose their only road to a clinic"** (Namuembi Medical Centre, Manafwa Red Cross). Verified NOT load-bearing on the D-027 ford assumption: reclassifying all 18 synth crossings from assumed-ford (most fragile) to bridge (least fragile) leaves `isolated_from` unchanged at `{clinic: 62, school: 3}`. All three blocking crossings — `w128611448` (operator), `w747829218` (osm), `w160219946` (osm) — are `structure=bridge`. | The number is network topology, not a fragility guess. Stating it pre-empts "your 18 guessed crossings drive your headline" |
| D-040 | WorldPop population NOT ingested; only 2 of 83 settlements carry a population prop. The pitch says "62 settlements", never a people-count. WorldPop deferred post-hackathon (rule 6). | An unverifiable number on stage is worse than a smaller verifiable one. "11,000 people" cannot be defended; 62 named villages can |
| D-041 | Operator injection name rule: the OSM `name` is preserved only when **non-empty**; otherwise the operator's CSV `name` wins. `w128611448` carries `noname=yes` with its name on `bridge:name`, so OSM name is NULL and the demo spine rendered as a bare way id. 09 → v0.9 **before** code (hard rule 3). | The spine must render "Manafwa Bridge (B112 town crossing)" on the map and in the why-chain. A way id on screen while the presenter says a name is a credibility leak |
| D-042 | 09 propagation step 5 corrected: the **road step appears in a why-chain only when a road_segment is actually SEVERED**. Under D-035 the bridge deck takes the impassable state and its approach roads take no state, so a road in every chain would name an unimpacted object. `propagate.py` was already correct; the spec was stale. **No code change.** | Invariant 2 requires the chain to be *true*, not merely long. Naming an OK road is a lie in the over-warning direction |
| D-043 | Playbook match is an EXACT `(object_type, state, hazard_kind)` triple — no wildcards. Multiple rows may share a triple; all fire, sorted by `(lead_time_hrs, owner_role, action_text)`. Actions are verbatim CSV text bound to one impact id. | A `*` row silently shadows a specific one, and "which row won?" must never be a question an officer asks. Sorted insertion extends invariant 1 from why-chains to the action list |
| D-044 | `fire_actions` returns `uncovered`: every impact with no playbook row, named and counted, never dropped. The loader validates every row against the ontology (`OBJECT_TYPES`, `STATE_ORDER` minus OK, new `ontology.HAZARD_KINDS`, non-empty owner, integer lead ≥ 0, no duplicates) and refuses any `ISOLATED` row containing a road-alternate phrase. ontology → v0.3 (hazard-kind registry); 09 → v1.0 | An impact that fires nothing renders as a red village with an empty action column — indistinguishable from "no action needed". That is the action-layer false all-clear (invariant 6). A typo'd `hazard_kind` would otherwise match nothing and raise nothing. An ISOLATED "take the detour" contradicts the engine's own proof and would route an officer over Old Manafwa bridge, which fails in the same flood (D-038) |
| D-045 | A blocked crossing fires its playbook action regardless of carrier count or dependent count. `actions_for()` orders by `consequence` (ISOLATED settlements naming the object in their why-chain) then `lead_time_hrs`, and flags `consequence==0` actions as `precautionary` — explained, never suppressed. `carriers` exposed on crossings. Guarded by a test that fails against a "skip zero-carrier crossings" tidy-up. | Zero dependents is a fact about OUR graph, not the world: 7 bare OSM ford nodes have no carrier within 100 m, and a missing road is a link-inference gap, not evidence no road exists. Suppressing the action would let a data gap silence a warning (invariant 6) — the failure this product exists to prevent. Ordering by lead time alone printed three unnamed 12 h ford closures above the 24 h B112 deck that cuts 51 villages: urgency is not consequence. Side effect: `w902422828` now reports `precautionary, carriers=1, consequence=0` — D-038 made machine-readable |
| D-046 | Corrects D-045's `consequence` derivation. It counted ISOLATED why-chains only. A settlement stores ONE chain, so a village that lost its clinic and its school records only the clinic — and both pilot schools reported `consequence=0, precautionary=True` while `isolated_from` said 3 settlements lost a school. Consequence now also reads each SERVICE_AT_RISK facility's own chain (which lists every settlement that lost it), charged to the facility ONLY, never to the bridges in that chain (whose list is a union across all its lost settlements — cross-charging would let `w128611448` claim the 8 villages `w747829218` blocked). Bridge counts unchanged: `w128611448` stays 51. | `precautionary` on a school that three villages just lost is an under-warning on the officer's screen, and the DEO deprioritises a shelter check. The engine was right — `propagate.py` wrote the victims into the facility's chain; the read-side derivation never looked. Guarded by two tests: a SERVICE_AT_RISK facility with 0 dependents fails; a forged two-bridge facility chain must not charge the innocent bridge |
| D-047 | Live GloFAS triggers. (a) A reach's GloFAS cell is OPERATOR-VERIFIED in `data/reach_glofas.csv` (`verified_by` mandatory), never auto-snapped, and the served cell is re-checked within 0.03° on every fetch. (b) Thresholds are empirical — Weibull plotting position over ANNUAL MAXIMA — capped at `MAX_RETURN_PERIOD=20`; the code raises rather than extrapolating, and `weibull_q` returns None beyond the record. (c) severity→T is data (`data/triggers.csv`): watch=Q2, alert=Q5, emergency=Q10, strictly increasing or refused. (d) Every feed failure raises; `USE_LIVE=0` returns an explicit disabled status. Manafwa @ Bubulo: 12.37 / 14.81 / 19.37 m³/s, emergency exceeded in 1997, 1998, 2002. | Measured, not assumed: the 4.3 km demo reach straddles three 0.05° cells reading 67.7 / 6.1 / 91.5 m³/s mean, non-monotonic downstream — two model a different river. Picking a cell by nearest vertex, or by *highest* discharge (a heuristic proposed and withdrawn), would have pinned the demo to the wrong water and every number after it would have been confidently wrong. The reanalysis serves from 1997, not the requested 1984, so n=29: Q10 rests on the third-largest peak and Q50 does not exist; Gumbel returns Q10=17.5 vs the empirical 19.4 by flattening the 1998 outlier. And a dead feed that returns "no hazard" is indistinguishable from a calm river — the exact failure this product exists to prevent |
| D-048 | Both demo endpoints raise `demo_flood_river()` (`scope=river`, D-036). `main.py` called `demo_flood()`, whose scope defaults to `reach`: every button press on the live deployment propagated at reach scope. The `alert` button is KEPT and captioned — at Q5 both town bridges score AT_RISK, nothing severs, nobody is isolated. | Reach scope measures 0 ISOLATED / 43 REROUTED on the pilot graph: villages detouring over crossings the same flood closes. A judge opening port 8017 saw a calm district produced by an engine that knew 62 villages were cut off — the exact failure this product exists to prevent, in the UI. The alert button is not a nothing-burger; it is the fragility table refusing to cry wolf, made clickable, and it is worth more on stage than the claim |
| D-049 | The UI reads impacts and actions for exactly ONE hazard — the newest active. The action list comes from `actions.actions_for()` and keeps its order. If >1 hazard is active the page says so and renders one. | The old action query joined `actions`→`impacts`→`objects` with no hazard filter and ordered by `lead_time_hrs`: on the live DB it rendered 852 rows across five hazards, three unnamed 12 h ford closures above the 24 h B112 deck that cuts 51 villages. Harmless in the button path (`clear_hazards()` wipes derived rows first), live everywhere else — CLI, cron, `scan_live`. On demo day the hazard is raised from the CLI |
| D-050 | Invariant 6 on the read side. (a) Every `STATE_ORDER` state must carry a colour — checked at import, never `.get(state, OK_GREEN)`. (b) The coverage panel always renders: `uncovered` impacts named (a committee choice), `unfired` impacts flagged as a BUG (a playbook row exists and no action fired), a failed playbook load shown in red. (c) The scan banner has four states: not run / disabled / feed failure / quiet-with-numbers. (d) A nameless crossing is labelled by its object id. (e) The footer attributes only OSM and GloFAS. | Five ways a page lies by omission while the engine is right. An unknown state rendered OK green; a red village with an empty action column read as "nothing to do"; a disabled scan read as a calm river; `(unnamed bridge)` was the same string for 32 objects including the one that cuts off 8 settlements; attributing WorldPop we never ingested is the same species of dishonesty as failing to attribute OSM. Each locked by a negative control |
| D-051 | Broadcast text claims NO arrival time and NO discharge number. `lead_time_hrs` is the playbook's completion deadline for an owner and is not a message slot. CAP `urgency` = `Unknown`, always. GloFAS `peak_date`/`peak_m3s` are NOT stored (option B cut under rule 6, post-hackathon). | I drafted "expected to be cut at {crossing} within {lead_time} hours" and it was false: 24 h is how long the district engineer needs, not how long the water takes. Rendering it as arrival time turns a logistics estimate into a flood forecast — on radio, in a language no judge can audit. Then the same fault, quieter: the return period and the 19.37 m³/s threshold are not columns, they are prose inside `trigger_detail`, and pulling them back out with a regex is read-side re-inference (the D-046 failure). A demo hazard has no numbers at all. Enforced by a slot whitelist in `ontology`, tested at the registry and not merely at one CSV row |
| D-052 | Local-language templates are committee data (`data/messages.csv`), never model output. `BROADCAST_STATES` (settlement/ISOLATED, bridge/IMPASSABLE, bridge/LIKELY_IMPASSABLE) is a versioned ontology rule, not a CSV accident. The AI edge drafts English polish and Swahili only, marked DRAFT. A `lum` row with no `en` row is refused; every impact with no template in a requested language is named. ontology → v0.4; 09 → v1.4 | Lumasaba reaches the last mile and is the language every model is worst at, and Bwayo is the only person who can audit it. A fluent-looking mistranslation of "do not cross" is an impact decision taken by a model (hard rule 1) in the one channel nobody can check. English is what the system degrades to, so a translation with nothing behind it degrades to silence. A village whose language was never written must appear as a gap, not as a warning that was sent |
| D-053 | The `ISOLATED` road-alternate guard matches DENIALS as well as offers. "There is no other road" is refused at load, exactly as "take the other road" is. Templates must state it positively: "It is the only road." | The guard is a substring blacklist and `in` cannot read English — negation flips meaning, a match does not see it. Discovered when the loader refused my own template. A false refusal costs one rewrite, caught at load, in front of the author. A false accept routes a village over Old Manafwa bridge, which fails in the same flood (D-038), and is caught by nobody. It refuses in the safe direction, and the wording it forces is the stronger one: the BFS proved the road is the only one |
| D-054 | CAP `instruction` carries the impact's playbook actions **verbatim**, ordered by `(lead_time_hrs, owner_role, action_text)`, and is the only CAP field `action_text` may enter. Every other CAP field is derived from something the engine already computed: `event` from the hazard, `severity` from `ontology.CAP_SEVERITY`, `certainty` = `Possible` when the blocking crossing's why-chain declares `assumed_structure:` (D-027) and `Likely` otherwise, `area` from the impacted object's name, `urgency` = `Unknown` always (D-051). Nothing is invented to fill a slot. | Prose would have to paraphrase committee words, and an impact can carry more than one action — the B112 deck carries two at 24 h — so "the action" is not a fact about an impact, and any prose rendering of it must either pick one or blur them. **Row reconstructed 2026-07-10 (Session 23):** the original was truncated mid-sentence in this file at `CAP \`instruction\` ca` and the remainder was overwritten by a duplicate D-055. Wording restored from the CAP table in `09_ontology_spec.md` v1.4, which this decision created. Superseded duplicate drafts of D-051, D-052 and D-055 removed in the same edit |
| D-055 | `messages_for()` returns `needs_name`: every crossing broadcast as a bare OSM way id, with the number of broadcasts naming it. The message still renders; the gap is reported. Fixed by an operator row in `operator_crossings.csv`, never by code. | An OSM way id on a map is a credibility leak; read aloud to a chief it is not a warning. 11 of 62 village broadcasts on the real graph say "the road crosses w747829218". Suppressing those messages would trade an unusable warning for no warning — worse. Rendering them silently would let a data gap reach a village unnoticed. A gap the officer can SEE is not the same failure as a gap nobody knows about (invariant 6), and it keeps the fix a CSV edit forever |
| D-056 | `app/ai_edge.py`: one adapter, one task (`translate`), one target language (`sw`). `target_lang="lum"` RAISES `AIEdgeRefused`. Every failure (no key, dead net, HTTP error, garbage draft) RETURNS a status and never raises — the exact inverse of `hazards.scan_live()`. Sanity checks: not empty, ≤3× / ≥0.3× source length, no markdown, no URL, and every engine-produced proper name must survive or the approver is warned. Temperature 0. Model env-configurable. Key in a header, never a URL. Drafts cached by (model, lang, text); failures never cached. `approved: False` always. urllib only, no new dependency. | A dead gauge that returns "no hazard" kills people; a dead translator costs a convenience — so one screams and the other shrugs. A refusal is different from a failure: nobody in the room can audit Lumasaba, so a fluent mistranslation of "do not cross" would be an impact decision taken by a model in the one channel nobody checks (hard rule 1). The sanity checks are what a NON-SPEAKER can verify: a model returning "Usivuke." dropped the instruction; one returning an essay added advice. Proper names come from the engine's `facts`, so the draft is checked against the graph. Free tier is ~10 RPM against 72 broadcasts, so the cache is not an optimisation. Google cut free quotas Dec 2025 and moved 3.x Pro to paid Apr 2026: a hardcoded model string is a demo that dies on someone else's release schedule |
| D-057 | `ai_edge` retries exactly once, after a 2 s pause, and only on HTTP **429 and 503**. Never on 400/401/403/404, never on a timeout, DNS failure, or any non-HTTP exception. The result dict carries `retried`. Worst-case wall time is 2×TIMEOUT_S + 2 s = 42 s; the realistic bound is one round trip + 2 s, because a 429/503 is a prompt response while a hang ends in a timeout, which is not retried. The docstring's "inside 20 s" is corrected in the same commit. | A live 503 on `gemini-2.5-flash` was observed in Session 22 — the most likely demo-day failure — and the free tier is ~10 RPM, so a 429 is a matter of when. A 429/503 means *come back*; a 4xx means *you are wrong*, and retrying it burns quota, doubles the latency of a certain failure, and delays the one diagnostic that matters: a rotated key not loaded shows as 403, and the presenter must see that in one second, not twenty-two. A retried hang would cost 42 s of a two-minute demo with no evidence the server would ever answer. The test that carries this decision is not "a 429 retries" but **"a 400 does not"**, asserted on the call count |
| D-058 | `AI_EDGE_LIVE` (default `0`) gates the AI edge, independently of `USE_LIVE`, which continues to gate the deterministic core alone. Disabled status is `edge disabled (AI_EDGE_LIVE=0)` and names the variable that would enable it. Demo-day configuration is `USE_LIVE=0, AI_EDGE_LIVE=1`. Refines D-008. | `USE_LIVE` exists because a dead gauge returning "no hazard" is indistinguishable from a calm river. That is a property of the FEED, not of liveness. The edge does not have it: a dead edge returns a named `edge unavailable` inside its timeout, and the English and Lumasaba text renders unchanged because neither ever passes through a model. The edge is safe by construction; `USE_LIVE` guaranteed that property by fiat. Coupled, the switches force a choice between the graph-sourced flood we need on stage (D-008) and the live Swahili draft we want. Four gate states now exist and none is dangerous — because `ai_edge` cannot see the graph, which D-059 stops asserting and starts enforcing |
| D-059 | The edge's payload is a **closed key set** `{text, target_lang, preserve}`; an unknown key raises `AIEdgeRefused`. The cache key hashes `SYSTEM` and the model alongside text and target language. A structural test parses `app/ai_edge.py` and refuses any module-level import of `propagate`, `actions`, `links`, `ontology`, `hazards` or `messages`; `db` (cache only) is the sole allowance. | Hard rule 1 says a model never sees an impact, and nothing enforced it: a later "pass `impact_id` so we can log it" would put a graph id into a prompt and no test would redden. Separately, the cache key covered only text+lang+model, so tuning `SYSTEM` after a rehearsal would silently serve drafts produced under the old instruction — a reproducibility hole in the one layer whose output a human signs off on. Both are one defect: something that changes what the model is told without changing what we can see |
| D-060 | The AI edge's order is `gate -> cache -> key -> provider`. A key is required to MAKE a draft, never to SERVE one, and the cache key does not contain it. New `cached_draft(text, lang) -> dict | None`: read-only, never calls the provider, never writes, returns `None` on a miss, forces `approved: False`, and is **ungated** — it is the only function a page render may call. `--selftest` takes its own text. Refines D-056/D-059. | Before this, an empty key blocked a cached draft while a garbage key sailed straight past it into a `DRAFT` — one of those two branches had to be wrong. Serving is not making: if `.env` fails to load on demo morning, the seventy-two rehearsed Swahili drafts still render, flagged `cached: True`. The gate stays in front of both, because `AI_EDGE_LIVE=0` means *do not use the edge* and a cache is part of the edge. `cached_draft` exists because a round trip to Google measured **11.3 s** on the pilot VPS — `getaddrinfo` returned a dead-routed IPv6 address and `urllib` walks that list in order, while `curl` races the families and looked healthy at 0.37 s — and because the free tier is ~10 RPM against 72 broadcasts. Sixty-two villages in a page render is an outage. `--selftest` takes text because a live probe that reuses a translated string never reaches the provider: it returns a cached DRAFT in 0.1 s and **looks like a pass**. That defeated two live checks in Session 23, both of guards that were in fact correct |
| D-061 | Phase 2 item 4c: the message panel. `main.py` reads `ai_edge.cached_draft()` in `home()` and NEVER `ai_edge()` — zero provider calls in a page render (a live translation is 4–6 s, D-060; 62 villages would be ~5 min of blocking). Two POST routes carry the only provider contact: `/draft/{impact_id}` calls the edge once and caches, `/approve/{impact_id}` signs the current draft. Approvals are a new table (`app/approvals.py`), keyed `(impact_id, lang, text_hash)` over the SWAHILI DRAFT BYTES — separate from the edge's draft cache on purpose (D-060): the cache stores what a model proposed, the table stores what a human signed, and a re-translation under a changed prompt (D-059) has a different hash, so an old signature never blesses new words. `messages.render()` gains `book=` so `messages_for()` parses the CSV once per page, not once per village. The Lumasaba `missing` list, the `needs_name` operator task list, and `errors` are each rendered by name (invariant 6): a village warned in a language nobody wrote, a crossing read aloud as a bare way id, must be visible, never swallowed. A cached `edge unusable` row is shown as REJECTED, never as a DRAFT a human could approve (D-056). | The panel is where the last-mile layer becomes visible on the map a judge sees, and it is the one screen that must never call a model in its render path: the free tier is ~10 RPM and a real call is 4–6 s, so a page that translates inline is an outage. Approval had to live outside the draft cache or it would collapse the wall the whole AI-edge doctrine rests on — proposed vs authorised. Binding the signature to the bytes means the committee re-approves when the words change, which is correct: they are signing a sentence, not an impact id. Two negative controls (home reaching the provider; a non-DRAFT row shown as the Swahili) came back GREEN in review and exposed test gaps, not code gaps — the home() error-guard was swallowing the trap — fixed by probing `broadcast_panel` on the uncaught path, per the Session 21/22 pattern |
