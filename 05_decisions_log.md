# 05 — Decisions Log

Append a line whenever a real decision is made. Newest at the bottom.

| ID | Decision | Why |
|----|----------|-----|
| D-001 | Ontology doctrine: typed objects + links + hazard propagation + action playbook; deterministic core, AI at edges only | The product IS the semantic model; wrong impacts/actions can cost lives — must be exact, explainable, testable |
| D-002 | SQLite two-table graph (objects, links) + pure-Python BFS; no graph DB, no networkx at MVP | District-scale graphs are small; keep installs trivial and the engine readable |
| D-003 | Port 8017 at /opt/lifeline, repo markbwayo/qvahlifeline, git-only workflow | Next free Qvah port; standard plumbing |
| D-004 | Pilot area decided by a Day-1 data check, not preference: candidate A = Marsabit/Isiolo corridor (research-verified coverage, Kenya NDMA relevance), candidate B = Mt Elgon corridor Mbale–Manafwa–Bududa (founder story, flood+landslide history). Check OSM asset density + GloFAS river points for both; pick the one that demos better; log result here | Depth in one convincing area beats breadth; don't ingest before locking |
| D-004 (result) | Day-1 data check run 2026-07-05 on VPS. B (Mt Elgon: Mbale–Manafwa–Bududa) wins decisively: 1107 assets/100km² vs 119 (Isiolo A); crossings 490 vs 156; usable GloFAS points 2 (peak ~7 m³/s) vs 0. Isiolo reads 0.0 m³/s at all river points — arid/drought corridor, no modeled perennial discharge, wrong hazard for a riverine-flood MVP. Founder authenticity (Mbale/Elgon, Lumasaba) reinforces. **PILOT REGION LOCKED = Mt Elgon.** | Data and story point the same way; a riverine-flood MVP needs a modeled river network, which only Elgon has |
| D-005 | Hazard feeds: GloFAS via Open-Meteo flood API + CHIRPS; triggers are watch/alert screening levels, stated honestly | Free, no-key, daily; 5 km resolution means we prioritise and explain, not predict per-metre |
| D-006 | Every impact stores its why-chain (ordered hazard→object link path); UI must render it | Explainability is the differentiator vs dashboards and vs black-box AI |
| D-007 | Playbook and fragility rules are versioned data (CSV / one table), never buried in code | Committees must be able to read, challenge, and tune them |
| D-008 | Demo must run with USE_LIVE=0 (cached/seeded data) | A dead network can never kill the pitch or the video |
| D-009 | Message templates aligned to CAP fields; English + one local language (AI-drafted, human-approved, marked as draft) | National adoption path + last-mile reality; AI never decides content of the impact, only wording |
| D-010 | Scope guard: submission on 30 July, one day early; any feature threatening that is cut and logged | Hackathons are lost to deadline slippage more than to missing features |
| D-011 | Post-event feedback capture (what actually happened per asset) is in the roadmap/pitch but NOT built for MVP | It's the moat later; it's scope creep now |
| D-012 | No Anthropic API in production; edge model default = Gemini free tier behind one adapter function | Cost + policy consistent across Qvah |
| D-013 | Ingest bbox ≠ data-check bbox. The 1108 km² check box holds ~12,266 objects (a whole region); ingesting it violates hard rule 4 (one legible district). Pilot tightened to a single river sub-catchment (~150–400 objects) chosen for a clean single-point-of-failure crossing. Exact corridor + anchor bridge: TBD (Bwayo to name) | Depth beats coverage; the demo must read as one district with one legible dependency chain |
| D-014 | Pilot corridor locked = Manafwa River @ Bubulo (Manafwa town); tight ingest bbox S0.884/W34.264/N0.929/E34.309 (~25 km²). Single-point-of-failure crossing NOT pre-named — selected by engineering judgement from the ingested crossings list. | No reliable named-bridge recall; data-first SPOF selection is more defensible and matches D-004 doctrine. Bbox provisional, tuned to the 150–400-object legibility target from ingest counts |
| D-015 | ingest_osm.py Step A = objects only, zero links. Crossings stored as type "bridge" + props.structure (bridge/culvert/ford/causeway) to match ontology.FRAGILITY. Full way geometry stored in props_json for Step B spatial inference. Idempotent wipe+reload. | Link inference feeds propagation and needs test cases (rule: nothing touches the engine untested); separating it keeps Step A incapable of producing a wrong impact |
| D-016 | A crossing = a road×river geometric intersection, not only an OSM-tagged bridge. OSM tagged just 2 bridges in the Manafwa box (both off-town, no culverts/fords). Step B will synthesise crossing objects from road/river geometry intersections; the engineer classifies demo-critical ones (bridge/culvert/ford) to set fragility. | Rural OSM under-tags crossings; the physical model is intersection-based. Geometry finds the crossing deterministically, the human sets the structure — hard rule 1 intact |
| D-017 | Pilot box corrected north to 0.905,34.260,0.962,34.305 (old box topped at 0.929, missed the Manafwa Town Council crossings). Operator ground-truth crossings recorded in data/operator_crossings.csv (source=operator): TC bridge (main road) + north footpath culvert, structures classified on satellite by the engineer. | The demo spine is operator-verified crossings, not OSM tags; rural OSM under-maps crossings, and the footpath culvert will never be in OSM |
| D-018 | A crossing object = a bridge/culvert/ford that CARRIES A VEHICLE ROAD (road_segment highway classes). Footbridges (highway=path) and stream/ditch/drain culverts are excluded at ingest. Rural OSM mapped ~80 such micro-crossings in the box (415 objects, 86 crossings); filtering to vehicle-road crossings gives ~16 real crossings, ~345 objects. Untagged road-over-river crossings recovered geometrically in Step B. Fords kept unfiltered (node-level, classic flood crossings). OSM confirms the operator's TC bridge = "Old Manafwa bridge" (tertiary). | LIFELINE models vehicle lifelines to clinics/markets; a footbridge over a ditch is not one. Keeps the district legible (rule 4) and Step B's graph clean |
| D-019 | Phase 1 Step A complete. Filtered ingest loaded 362 objects for box 0.905,34.260,0.962,34.305: 16 crossings (6 road bridges incl. Old Manafwa bridge/tertiary = operator TC bridge; 10 fords), 4 clinics, 83 settlements, 20 schools, 85 river reaches, 154 roads. --refresh needed due to box-only cache key (fix pending). | Clean, legible district graph; demo spine (TC bridge) OSM-confirmed |
| D-020 | Hardened ingest_osm.py: (a) Overpass cache key includes a hash of the query text, so a changed query auto-refetches and stale-query results can't silently return; (b) each bucket retries across 2 mirrors with exponential backoff (5/10/20s) and isolates on failure (returns FAILED, no crash) — successful buckets stay cached so a re-run retries only failures; (c) a real load aborts (exit 1) rather than write a graph missing a whole category. --refresh now rarely needed. | The box-only cache key served the stale 86-crossing set; one slow bucket crashed a full run. Both traps would bite Step B, which refetches |
| D-021 | Map renders road_segment + river_reach as polylines from stored props.geometry (rivers blue; roads grey when OK, state-coloured and thicker when impacted); point assets + crossings stay as dots on top. No propagation/fragility change. | 362 dots was illegible; lines make it read as a district (rule 4) and pre-wire the severed-road visual for Step B |
| D-022 | Manafwa town crossing is TWO real bridges 19.8 m apart on the SAME reach w188321163: w128611448 (Manafwa Bridge, B112 secondary, main tarmac) and w902422828 (Old Manafwa bridge, C870 tertiary). The demo SPINE = w128611448 (main tarmac; operator satellite point falls on it) — corrects earlier docs that named "Old Manafwa bridge". Old C870 kept as an OSM vehicle crossing. Clustered-crossing reconciliation: operator_crossings.csv match_hint (OSM id) resolves identity; injection refuses on an ambiguous cluster with no hint | Nearest-pick silently decided a spine's identity; two named bridges with different refs/road-classes are two structures; both co-fail (same reach) so the SPOF is the crossing pair, not one bridge — reachability (sub-step 5) confirms south-bank isolation |
| D-023 | Geometric crossing synthesis: vehicle road×river intersections with no crossing within ~50 m create source=synth, needs_review=True, structure-less candidates (provenance stored). Structure/fragility never guessed, so they yield no impact until a human classifies them. Vehicle highway classes only; ~50 m self+existing dedup; idempotent | Rural OSM under-tags crossings: a missed crossing risks a false ISOLATED (over-warning), a guessed structure risks a false all-clear (under-warning). Find them by geometry, leave classification to the human. Propagation treatment of unclassified crossings deferred to sub-step 3 |
| D-024 | crosses = nearest river_reach within ~50 m of the crossing point; carries = every vehicle road whose line passes within ~15 m. Footpath crossings get crosses but never carries (vehicle-graph gate). Synth (needs_review) crossings get both, so an unclassified crossing can sever a route. Idempotent (rebuilds only geom_* links). | 15 m is tuned below the 19.8 m town-bridge separation so the two clustered bridges don't cross-link carriers (~5 m margin — verified on the real graph); it still captures both segments of a road that splits at a bridge (~0 m). Whether an unclassified crossing actually severs is the conservative fragility rule, deferred to the propagation step |
| D-025 | Reachability graph inference: connects = vehicle roads sharing a vertex (~1 m grid; footpaths excluded so the network is vehicle-only); access_via = each settlement AND serving facility attaches to its nearest vehicle road; serves = each settlement to the nearest facility of each type within an 8 km catchment cap (committee-editable). All idempotent (rebuild only geom_* links). Verified end-to-end through propagate.py: settlement whose only clinic route crosses a blocked culvert -> ISOLATED. | The BFS needs a vehicle-only road graph, an entry point per settlement/facility, and a catchment so it knows which facility a settlement depends on. Footpath exclusion prevents a footbridge silently keeping a route open |
| D-026 | carries threshold raised 15m->20m: real measurement on the live graph showed the demo-spine bridge w128611448's true nearest road at 17.7 m (excluded by 15 m - a wrong-impact bug, the spine could never sever its road) while the OTHER town bridge's road sat safely at 23.9 m. 20 m sits inside that real gap. Added a nearest-road fallback (cap 100 m, flagged geom_carries_fallback) for any crossing left with zero carriers after the primary pass, so an unlinked crossing can't silently fail to sever a route it's actually on. | A crossing with no carrier can never produce a severed road - if it's the only route to a clinic, propagation would miss the isolation. Threshold tuned from measured data, not guessed; fallback closes the residual risk for less-clustered crossings elsewhere in the district |
| D-027 | Unclassified crossings (structure None/unknown) are scored as the MOST fragile structure (ford), not the least. Fixes bridge_state's `structure or "bridge"` fallback and propagate.py's `props.get("structure","bridge")` default - two layers of the same wrong assumption, which at alert severity returned AT_RISK (non-severing) instead of LIKELY_IMPASSABLE (severing), so a flood over an unclassified crossing severed no road and isolated no village: a false all-clear. Why-chain now declares `assumed_structure:ford(unclassified)`. ontology -> v0.2. | Base rate: an unmapped rural crossing is more likely a ford/culvert than an engineered bridge. Asymmetric cost: a false "may be out" costs an inspection; a false "all clear" can kill. Applies to all 18 synth crossings. Known structures unchanged (an engineered bridge at watch is still OK) - conservative, not indiscriminate |
